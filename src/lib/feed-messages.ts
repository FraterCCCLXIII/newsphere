/** Placeholder copy until RSS ingestion is implemented (shown inside each column card). */
export const COLUMN_RSS_PLACEHOLDER =
  "RSS ingestion is planned for a later phase. This column is ready for your feed.";

/** Same phase note when the grid has no columns yet. */
export const GRID_EMPTY_RSS_NOTE =
  "RSS ingestion is planned for a later phase. Add columns in Settings when you are ready.";
